Общие библиотеки Linux
=========================

Shared libraries (`.so` файлы)  важнейшая часть userspace.  
Они позволяют приложениям использовать внешние функции (например, из glibc, libm, OpenSSL), не встраивая их в каждый бинарник.

.. toctree::
   :maxdepth: 1
   :caption: Подразделы

   overview
   layout
